#pragma once

class Console
{
public:
	Console();
	virtual ~Console();
};

// TODO
/*
	console for virtual console in Engine
	classes for Actions
	classess ACTION with TARGETS [do(), undo()] -> look design patterns ebook
	queue ACTIONS for undo

	SETTINGS FILE FOR COMMANDS INIT 
	for example: SET something value
*/